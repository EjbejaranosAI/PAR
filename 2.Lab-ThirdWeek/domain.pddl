(define (domain wompusWorld)
        (:requirements :strips :adl)
        (:predicates
            ;predicaators for the world wompusWorld
            (adj ?r1 ?r2)
            (at ?r1 ?r2)
            (pit ?r)
            (wompus ?x ?t ?x)
            (gold ?x ?t ?x)
            (blank ?x ?y)
            (have ?ag ?g)
            (dead ?w)


        ))

        